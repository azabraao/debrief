# X/Twitter launch thread (paste-ready)

**Tweet 1 (the hook — post with the home screenshot):**

Last night I gave Claude Code one instruction:

"build a product people will love. I'll see it in the morning."

Then I went to sleep.

I woke up to a finished product — and its first user was me, checking what the AI did all night.

It built the tool for exactly that. 🧵

**Tweet 2 (what it is — recorder screenshot):**

Debrief is a flight recorder for AI coding agents.

It reads the transcripts Claude Code already writes and answers the 4 morning questions:

→ what needs me? (with the exact question quoted)
→ what did each session do?
→ what did it cost?
→ what exactly happened at 3am?

**Tweet 3 (the demo):**

One command. No account. Nothing leaves your machine.

npx debrief-cli

It indexed 1.4GB of my transcript history in 4.8 seconds and told me my sessions did $8,400 of API-equivalent work last month.

(Max subscribers: this number will make you feel great)

**Tweet 4 (the meta moment — screenshot of the session that built it, optional):**

The part that gets me:

while it was building the dashboard, it pointed the dashboard at itself.

Debrief's first debrief was the session that built Debrief — status: ● running, 17 files, $28 of work.

The flight recorder recorded its own first flight.

**Tweet 5 (honest details):**

Under the hood, because you'll ask:

- zero runtime dependencies (the entire npm package)
- streams gigabytes of JSONL in seconds, warm boots in 40ms
- deterministic — no LLM summarizing your sessions
- costs are token-accurate incl. cache reads/writes per model

**Tweet 6 (CTA):**

Free preview today. $29 founding license if it earns a place in your mornings.

npx debrief-cli

[link]

---

Notes: post 8–10am PT weekday. Attach real screenshots from `--demo` mode only.
Reply to your own tweet 1 with the GitHub/site link 10 min after posting (algo).
